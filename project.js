

        // Call initAll when the window loads
        window.onload = initAll;
            function handleFormSubmit() {
            // Add your form validation logic here if needed
             
            // Display alert box
            alert("You are successful booking. Thank you!");

            // Return false to prevent the form from actually submitting
            return false;
 }
		